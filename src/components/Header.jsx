import React from "react";
import CreateIcon from "@material-ui/icons/Create";

function Header() {
  return (
    <header>
      <h1>
        <CreateIcon />
        iNote
      </h1>
    </header>
  );
}

export default Header;
